version: 0.2
phases:
  install:
    runtime-versions:
        python: 3.8
  build:
    commands:
      - pip install --upgrade pip
      - pip install pipenv --user
      - pipenv install awscli aws-sam-cli
      - pipenv run sam package --template-file test2/test.yaml --s3-bucket xrdeploymentest-ffmpegsourcecode-ap-northeast-1-652549287100
